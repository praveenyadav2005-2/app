# Backend Environment Variables
# Update these for production deployment

NODE_ENV=production
PORT=5000

# MongoDB Connection (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/enigma-game?retryWrites=true&w=majority

# JWT Secret (generate strong random string: use `openssl rand -hex 32`)
JWT_SECRET=your-strong-random-secret-key-here

# Frontend URL (update with your Vercel domain)
FRONTEND_URL=https://your-app.vercel.app

# CORS Origins (comma-separated, use frontend domain in production)
CORS_ORIGINS=https://your-app.vercel.app
